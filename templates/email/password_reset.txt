{{ product_name }} — Reset Your Password

We received a request to reset the password for your account. Click the link below to set a new password:

{{ reset_url }}

This link expires in 30 minutes. If you didn't request a password reset, you can safely ignore this message — your password will not be changed.
{% if support_email.is_some() %}
Need help? Contact {{ support_email.as_deref().unwrap_or_default() }}
{% endif %}
{% if custom_footer_text.is_some() %}{{ custom_footer_text.as_deref().unwrap_or_default() }}{% endif %}

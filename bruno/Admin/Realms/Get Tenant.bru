meta {
  name: Get Realm
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/realms/{{realmId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

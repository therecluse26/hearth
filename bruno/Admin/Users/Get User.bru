meta {
  name: Get User
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/users/{{userId}}
  body: none
  auth: bearer
}

headers {
  X-Tenant-ID: {{tenantId}}
}

auth:bearer {
  token: {{accessToken}}
}

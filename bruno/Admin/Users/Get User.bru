meta {
  name: Get User
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/users/{{userId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

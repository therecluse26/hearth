meta {
  name: List Users
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/users
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

query {
  page: 1
  per_page: 20
}

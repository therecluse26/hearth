meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/users/{{userId}}
  body: none
  auth: bearer
}

headers {
  X-Tenant-ID: {{tenantId}}
}

auth:bearer {
  token: {{accessToken}}
}

meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/users/{{userId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

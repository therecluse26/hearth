meta {
  name: Delete Tenant
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/tenants/{{tenantId}}
  body: none
  auth: bearer
}

headers {
  X-Tenant-ID: {{tenantId}}
}

auth:bearer {
  token: {{accessToken}}
}

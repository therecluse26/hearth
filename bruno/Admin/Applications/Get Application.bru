meta {
  name: Get Application
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/applications/{{clientId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

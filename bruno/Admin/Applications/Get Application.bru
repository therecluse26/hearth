meta {
  name: Get Application
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/applications/{{clientId}}
  body: none
  auth: bearer
}

headers {
  X-Tenant-ID: {{tenantId}}
}

auth:bearer {
  token: {{accessToken}}
}

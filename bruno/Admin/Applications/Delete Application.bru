meta {
  name: Delete Application
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/applications/{{clientId}}
  body: none
  auth: bearer
}

headers {
  X-Tenant-ID: {{tenantId}}
}

auth:bearer {
  token: {{accessToken}}
}

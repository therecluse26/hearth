meta {
  name: Revoke Token
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/revoke
  body: json
  auth: none
}

headers {
  X-Tenant-ID: {{tenantId}}
  Content-Type: application/json
}

body:json {
  {
    "token": "{{accessToken}}",
    "token_type_hint": "access_token",
    "client_id": "{{clientId}}"
  }
}
